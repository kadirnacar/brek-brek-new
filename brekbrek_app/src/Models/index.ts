export { MainCarouselImages, Settings, MailSettings, MailTemplate } from './Settings';
export { Channels, Contacts, Users } from './Channels';

import { Channels, Contacts, Users } from './Channels';

export const Models = {
  Contacts,
  Channels,
  Users,
};
